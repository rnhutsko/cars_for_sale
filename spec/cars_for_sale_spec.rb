require 'spec_helper'

describe CarsForSale do
  it 'has a version number' do
    expect(CarsForSale::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
